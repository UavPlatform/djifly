package com.fuwaki.djifly.ui.theme

import androidx.compose.ui.graphics.Color

// ── 品牌色（深色系 + 天空蓝） ──
val AccentBlue = Color(0xFF38BDF8)       // 天空蓝 - 主强调色
val AccentBlueDark = Color(0xFF0EA5E9)
val SurfaceDark = Color(0xFF0A0F14)      // 暗色背景
val SurfaceCard = Color(0xFF141A20)      // 卡片背景
val SurfaceCardLight = Color(0xFF1C2430) // 较亮卡片

// ── 状态色 ──
val StatusGreen = Color(0xFF10B981)      // 成功/在线/完成
val StatusAmber = Color(0xFFF59E0B)      // 警告/等待
val StatusRed = Color(0xFFEF4444)        // 错误/离线
val StatusBlue = Color(0xFF3B82F6)       // 信息/执行中

// ── 文字色 ──
val TextPrimary = Color(0xFFF1F5F9)      // 主文字
val TextSecondary = Color(0xFF94A3B8)    // 次文字
val TextTertiary = Color(0xFF64748B)     // 辅助文字

// ── 分隔线 ──
val Divider = Color(0xFF1E293B)

// ── 保留原有 Material 主题色（兼容） ──
val Purple80 = Color(0xFFD0BCFF)
val PurpleGrey80 = Color(0xFFCCC2DC)
val Pink80 = Color(0xFFEFB8C8)
val Purple40 = Color(0xFF6650a4)
val PurpleGrey40 = Color(0xFF625b71)
val Pink40 = Color(0xFF7D5260)
